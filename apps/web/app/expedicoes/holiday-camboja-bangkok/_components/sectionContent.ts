export type ImageWithLocationContent = {
  src: string;
  alt: string;
  name: string;
  country: string;
};

export type HeadingContent = {
  prefix: string;
  highlight: string;
  breakBeforeHighlight?: boolean;
};
